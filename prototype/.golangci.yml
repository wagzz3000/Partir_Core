run:
  timeout: 5m
  tests: false

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - typecheck
    - unused
    - misspell
  disable-all: true

linters-settings:
  govet:
    check-shadowing: true
  gofmt:
    simplify: true
